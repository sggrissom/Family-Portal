import { block } from "vlens/css";

block(`
.appearance-item {
  align-items: flex-start;
}
`);

block(`
.appearance-item .event-item-main {
  flex: 1;
}
`);

block(`
.appearance-editing {
  flex: 1;
  min-width: 0;
}
`);
