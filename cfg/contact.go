package cfg

// SupportEmail is the address published on /support and linked from the error
// pages. It lives here rather than in a page component so that the address
// appears in exactly one place; nothing about it differs between local and
// release builds.
const SupportEmail = "support@familyrecord.app"
